//import n2words from "n2words";

function moneyToWords(money: number, lang: "pl" | "en"): string {
  return ""; //n2words(money, { lang });
}

const test = moneyToWords(1000, "en");
console.log(test);
